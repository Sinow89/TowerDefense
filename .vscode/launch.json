{
    "configurations": [
                {
            "cwd": "${workspaceFolder}",
            "type": "cppdbg",
            "request": "launch",
            "name": "Launch MyGame",
            "program": "${workspaceFolder}/MyGame.exe",
            "MIMode": "gdb",
            "preLaunchTask": "C/C++: zig.exe build active file"
        }      
    ]
}